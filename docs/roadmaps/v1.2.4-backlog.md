# Codero v1.2.4 — Backlog Ticket & Scope Spec

**Status:** In progress (deferred from v1.2.3)
**Release type:** UI modernization + incremental hardening + feedback loop hardening
**Created:** 2026-03-18
**Updated:** 2026-03-18 (status sync after COD-NEW-A/B/C merges + LOG-001/UI-003 additions + gate-checks/active-session parity + COD-059 dashboard live fields)

---

## Scope Summary

v1.2.4 focuses on the TUI and dashboard visual refresh work explicitly deferred from v1.2.3,
plus three feedback-loop hardening items surfaced by the TEST-1/TEST-2/full-cycle pilots:
- COD-NEW-A: post-push CI watcher
- COD-NEW-B: pre-push test gate
- COD-NEW-C: autonomous finish-loop orchestrator (codero-finish.sh)

v1.2.3 delivered functional correctness (BUG-001 fix, surface parity harness). v1.2.4 delivers
the operator-facing visual quality improvements that were out of scope for v1.2.3, closes
the CI feedback gap with finish-loop automation, and defines the structured heartbeat/log
shape needed for the UI to render per-step passing/failing/disabled states and current
activity/session context.
The per-step feed should come from the canonical gate-check report (`/api/v1/dashboard/gate-checks`),
not a parallel ad hoc `gate-steps` surface. Current activity should come from fresh
branch-heartbeat rows, with task/phase context filled in only when the backend can resolve it.

Current status snapshot:
- ✅ COD-NEW-A completed (merged in PR #60)
- ✅ COD-NEW-B completed (merged in PR #60)
- ✅ COD-NEW-C completed (merged in PR #62)
- ✅ COD-059 completed (PR open — dashboard live fields)
- ⏳ COD-055 in progress (PR #63 open)
- ⏳ UI-001, UI-002, UI-003, INFRA-001, LOG-001, DOC-001 remain open

---

## Backlog Items

### COD-059: Dashboard Live Fields — Security Score, Coverage, ETA, PR Number, OwnerAgent (P1)
**Surface:** Dashboard (web) + state DB
**Owner:** ClaudeCode
**Status:** Completed (PR open)
**Depends on:** main (post COD-057 merge)

**Description:** Replace the five hardcoded/stub fields in the dashboard and TUI with live data
sourced from the DB and filesystem:
1. **Security Score** (`sr-score` / `sr-pct`) — derived from `.codero/gate-check/last-report.json`
2. **PR Number** (`tb-pr-num`) — persisted in `branch_states.pr_number` by the reconciler
3. **Coverage %** (`stat-coverage`) — parsed from `coverage.out` at repo root
4. **ETA** (`stat-eta`) — derived from `review_runs` history (avg duration − elapsed)
5. **OwnerAgent** — read from `branch_states.owner_agent`; populated via `UpdateOwnerAgent()`

**What was implemented:**
- DB migration 000004: adds `pr_number INTEGER` and `owner_agent TEXT` columns to `branch_states`
- `state.UpdatePRNumber()` / `state.UpdateOwnerAgent()` repository functions
- Reconciler calls `UpdatePRNumber()` on every successful GitHub poll
- New `GET /api/v1/dashboard/health` endpoint returns `DashboardHealth` JSON:
  - `security_score` (score 0–10, pct, critical/high counts)
  - `coverage_pct` (parsed from `coverage.out`)
  - `eta_min` (best-effort from `review_runs`)
- `queryActiveSessions()` now scans `pr_number` + `owner_agent` from DB
- Dashboard HTML: new security panel, stats panel, `tb-pr-num` in nav
- Dashboard JS: `refreshHealth()` and `refreshActiveSessions()` as non-fatal side-effects

**Acceptance criteria:**
- `go build ./...` clean ✅
- `go test ./internal/state/... ./internal/dashboard/... ./internal/webhook/...` all pass ✅
- Security score shows from gate-check report; hides when report absent
- Coverage shows from `coverage.out`; hides when file absent
- ETA hides when no `review_runs` history
- PR number updates in nav bar when active session has a known PR
- `OwnerAgent` populated when `CODERO_AGENT_ID` env var is set before `UpdateOwnerAgent()` is called

---

### COD-060: OwnerAgent Population at Session Start (P2)
**Surface:** Daemon / `codero queue` / heartbeat path
**Owner:** ClaudeCode
**Status:** Not started
**Depends on:** COD-059

**Description:** COD-059 added `branch_states.owner_agent` and the `UpdateOwnerAgent()` function,
but no call site was added in the daemon/session init path. The field remains empty until
an agent explicitly calls it. This item wires the call into the existing session lifecycle.

**Acceptance criteria:**
- When a session starts (`codero queue` / daemon branch acquisition), reads `CODERO_AGENT_ID`
  env var; falls back to `$USER` or hostname
- Calls `state.UpdateOwnerAgent(db, repo, branch, agentID)` at session start and on heartbeat
- Dashboard active-sessions panel shows the resolved agent ID instead of blank
- Existing heartbeat tests continue to pass; new test verifies the field is set

---

### COD-061: Coverage Upload Integration (P2)
**Surface:** Dashboard / CI integration
**Owner:** ClaudeCode
**Status:** Not started
**Depends on:** COD-059

**Description:** COD-059 reads `coverage.out` from `CODERO_REPO_PATH` (defaulting to `.`).
In CI the coverage file is generated in a temp directory and not always at the repo root.
This item adds a `CODERO_COVERAGE_PATH` env override so CI can point to the generated file,
and adds a `POST /api/v1/dashboard/coverage-upload` endpoint so agents can push coverage
data without needing filesystem access to the dashboard server.

**Acceptance criteria:**
- `CODERO_COVERAGE_PATH` env var overrides the default `{repo_root}/coverage.out` path
- `POST /api/v1/dashboard/coverage-upload` accepts a `coverage.out` file upload, stores in
  a configured path, and returns the parsed coverage % in the response
- Existing `parseCoverageFile()` is unchanged; upload handler calls it after writing the file
- `GET /api/v1/dashboard/health` reflects the uploaded file immediately
- Existing health endpoint tests continue to pass

---

### COD-062: ETA Calibration and Per-Repo History (P2)
**Surface:** Dashboard / `review_runs` DB
**Owner:** ClaudeCode
**Status:** Not started
**Depends on:** COD-059

**Description:** COD-059 derives ETA from a simple `AVG((finished_at - started_at) * 1440)` over
completed runs in the last 7 days. For repos with mixed workload sizes (quick fixes vs. full
refactors), the global average is noisy. This item splits the history by branch-prefix heuristic
and adds p50/p90 percentile exposure alongside the raw ETA.

**Acceptance criteria:**
- `queryETAMinutes()` now returns a richer struct: `{ avg_min, p50_min, p90_min, elapsed_min, eta_min }`
- `DashboardHealth.ETADetail` (new field, old `ETAMin` kept for backwards compat)
- Dashboard shows p50 ETA with a `±` annotation when p90 diverges by more than 20%
- Existing `?repo=` filter continues to work; branch prefix filter added as `?branch_prefix=`
- New unit test exercises the percentile computation with a synthetic `review_runs` fixture

---

### UI-001: TUI Visual Design Refresh (P1)
**Surface:** TUI (Bubble Tea)
**Owner:** ClaudeCode / UI agent
**Status:** In progress (shell foundation in place; remaining work is event stream, merge footer, live agent rows, findings detail, assistant context, detail/history tabs, and footer polish)
**Description:** Modernize the Bubble Tea layout for the `codero tui` and `codero gate-status`
views. Current layout is functional but visually dense; the next slice should focus on turning the
TUI into a clearer operator shell rather than adding more one-off pane styling.

**Implementation direction (2026-03-20):**
- Preserve the existing Codero-specific surfaces:
  - review assistant shell
  - findings & routing pane
  - pipeline pane
  - event stream & architecture pane
- Prioritize shell-level improvements first:
  - compact top operator strip with gate/findings/agent/review state
  - shared panel and metric primitives for all panes
  - mode-aware bottom action bar
  - stronger selected-row and active-pane styling
  - centered help/detail overlays where appropriate
- Borrow patterns from proven terminal UIs:
  - `gh-dash` for Bubble Tea workflow and keyboard/action ergonomics
  - `campfire` for event/log rendering
  - `kue` for overview/detail overlay flow
  - `lazygcs` for search/preview navigation
  - `k9s` and `lazydocker` for dense operator framing
  - `Helius` for visual density/status-bar ideas only, not code

**Acceptance criteria:**
- Gate view uses clear status icons with colour-coding (pass=green, fail=red, skip=yellow, disabled=grey)
- Summary line is visually distinct from the per-check table
- Layout degrades gracefully in narrow terminals (< 80 cols)
- `--tui-snapshot` output remains deterministic and parseable (no ANSI escape sequences)
- All existing TUI/JSON parity tests pass
- New TUI chrome remains consistent across panes (shared headers, panel blocks, and selected-row treatment)
- Mode/help text clearly explains active actions without relying on external docs

**Reference borrow matrix (2026-03-20):**

**Tier 1 — Primary implementation references**
- [`leg100/pug`](https://github.com/leg100/pug)
  - strongest long-term shell reference for Codero
  - borrow: pane manager architecture, focusable split panes, numeric pane focus, resize controls, explorer/detail/log/task coordination
  - Codero targets: findings explorer + detail, persistent event/log pane, resizable multi-pane shell
- [`charmbracelet/soft-serve`](https://github.com/charmbracelet/soft-serve)
  - strongest shared-primitives reference
  - borrow: shared `Common` UI core, footer/help contract, status bar value/info contract, tabs, selector + detail split, markdown/code rendering
  - Codero targets: shared pane primitives, page-level help model, findings/activity/architecture/review tab contract
- [`GustavoCaso/docker-dash`](https://github.com/GustavoCaso/docker-dash)
  - strongest operator-dashboard reference
  - borrow: top navigation tabs, switchable right-side panels, contextual bottom help/status bar, confirmation modals, alert banners
  - Codero targets: top-level Findings/Pipeline/Agents/Logs navigation, right pane detail switcher, routing/review confirmations
- [`cloudposse/atmos`](https://github.com/cloudposse/atmos)
  - strongest multi-column workflow reference
  - borrow: 3-column operator flow, focused-column border treatment, `/` filtering, overview/detail toggle, markdown/table chat rendering
  - Codero targets: findings/detail/actions shell, right-pane preview toggles, stronger mode-aware footer

**Tier 2 — Strong pane and workflow references**
- [`gitsocial-org/gitsocial`](https://github.com/gitsocial-org/gitsocial)
  - closest conceptual match for review workflows
  - borrow: host/router architecture, shared TUI core, review-specific flows for list/detail/diff/feedback, reusable confirm and nav components
  - Codero targets: review workflow routing, finding/detail/feedback transitions, shared host/view registry patterns
- [`dhth/prs`](https://github.com/dhth/prs)
  - strong review-centric Bubble Tea v2 reference
  - borrow: PR list + timeline + detail split, help view, status styling, markdown detail rendering, focus-aware pane switching
  - Codero targets: findings timeline/detail, activity list/detail, markdown-heavy review detail panes
- [`BalanceBalls/nekot`](https://github.com/BalanceBalls/nekot)
  - strongest prompt and operator-strip reference
  - borrow: compact status strip, prompt/input pane discipline, searchable side lists, text selection/copy workflow
  - Codero targets: top operator strip, bottom review assistant, searchable findings/agents lists, copy-friendly log/finding views
- [`dhth/cueitup`](https://github.com/dhth/cueitup)
  - strongest simple list/detail inspection reference
  - borrow: active-pane highlighting, footer mode chips, separate help view, focused inspector flow
  - Codero targets: findings list/detail, event log/detail, footer mode chips
- [`kontrolplane/kue`](https://github.com/kontrolplane/kue)
  - strongest lightweight overview/detail/filter reference
  - borrow: table-driven overview pages, filter bar, centered help/confirm overlays, clean detail transitions
  - Codero targets: filterable findings and agent tables, quick help overlay, lightweight create/confirm/review action flows
- [`dukaev/superdive`](https://github.com/dukaev/superdive)
  - strongest dense inspection reference
  - borrow: tree/detail coupling, async panel loading, reusable pane contracts, viewport-heavy detail surfaces
  - Codero targets: findings tree/detail, architecture/evidence inspection, richer detail panes
- [`Rshep3087/lunchtui`](https://github.com/Rshep3087/lunchtui)
  - strongest TUI-overview dashboard reference
  - borrow: metric hero cards, responsive overview sections, period navigation, help-toggle footer, and mixed list/table/detail flows
  - Codero targets: top summary strip plus metric cards, overview-style findings/health sections, time/period navigation where useful, cleaner config/status views
- [`charmbracelet/glow`](https://github.com/charmbracelet/glow)
  - strongest markdown/detail rendering reference
  - borrow: markdown pager ergonomics, polished help/status bars, adaptive rendering for rich text
  - Codero targets: review assistant output, help overlay, finding detail, architecture explanation pane

**Tier 3 — Focused utility references**
- [`purpleclay/dns53`](https://github.com/purpleclay/dns53)
  - borrow: page-owned help bindings, filtered list component, simple header/page/footer framing
  - Codero targets: filtered findings/agents lists, per-pane help ownership
- [`ariasmn/ugm`](https://github.com/ariasmn/ugm)
  - borrow: compact tab-to-switch list/detail flow, searchable list + viewport pairing, and embedded detail tables via `bubble-table`
  - Codero targets: quick findings/agents toggles, side-list plus detail viewport panes, lightweight embedded metadata tables
- [`dhth/outtasync`](https://github.com/dhth/outtasync)
  - borrow: compact inline status badges, footer counts, quick filtered-view toggles
  - Codero targets: blockers/failed/all toggle rail, compact summary counters
- [`caarlos0/fork-cleaner`](https://github.com/caarlos0/fork-cleaner)
  - borrow: bulk-select list workflow, dense summary rows, simple extra-help keys
  - Codero targets: bulk finding actions, bulk agent/session selection
- [`stefanlogue/meteor`](https://github.com/stefanlogue/meteor)
  - borrow: focused, low-friction form flow and config-driven task scaffolding
  - Codero targets: compact guided action flows for quick review/routing tasks, not the main shell
- [`a3chron/gith`](https://github.com/a3chron/gith)
  - borrow: polished quick-action flows, theme/accent discipline, step-based shortcut paths
  - Codero targets: review quick actions, operator presets, guided triage shortcuts
- [`chhetripradeep/chtop`](https://github.com/chhetripradeep/chtop)
  - borrow: compact telemetry walls and lightweight refresh surfaces
  - Codero targets: agent health or queue telemetry strip/pane
- [`khaapamyaki/at_cli`](https://github.com/khaapamyaki/at_cli)
  - borrow: tiny focused input/result flows only
  - Codero targets: very small modal command flows, not the main shell

**Inspiration only — do not port code**
- [`STVR393/helius-personal-finance-tracker`](https://github.com/STVR393/helius-personal-finance-tracker)
  - use for visual density, status-strip rhythm, metric cards, and centered overlays
  - do not reuse code; AGPL-only reference
- [`kyren223/eko`](https://github.com/kyren223/eko)
  - use for vim-style input handling, focus/blur border treatment, modular panel composition, and choice/help popup patterns
  - do not reuse code; AGPL reference and chat-first product model
- [`bensadeh/circumflex`](https://github.com/bensadeh/circumflex)
  - use for custom list/status/pagination/help bubble patterns, native-terminal color discipline, and optional handoff of long-form detail to an external pager
  - do not reuse code; AGPL reference and reader-first product model
- [`termkit/gama`](https://github.com/termkit/gama)
  - use for live mode toggles, tab locking, and confirmation-strip ideas
  - do not reuse code; GPL reference
- [`rusinikita/trainer`](https://github.com/rusinikita/trainer)
  - use for adaptive list/play/detail flow, help footer rhythm, and lightweight guided-learning pane ideas
  - do not reuse code; GPL reference

**Codero adoption plan from the reference set**
- Shell/navigation pass:
  - base the next shell rewrite on `pug + soft-serve + docker-dash + atmos`
  - goals: compact top operator strip, shared pane primitives, stronger focus treatment, pane resize/focus controls, mode-aware footer
- Findings/detail pass:
  - base the findings workflow on `cueitup + superdive + prs + glow`
  - goals: clearer list/detail split, richer markdown detail rendering, timeline/detail transitions, dense but readable selection state
- Review assistant pass:
  - base the assistant on `nekot + atmos + glow`
  - goals: better prompt bar, clearer current-context strip, improved markdown/table output, copy-friendly responses
- Event/log pass:
  - base the activity pane on `superdive + prs` and earlier `campfire` notes
  - goals: stronger event timeline, severity/status cues, readable detail drilldown
- Utilities:
  - use `dns53`, `outtasync`, and `fork-cleaner` for filtered lists, compact counters, and bulk-action patterns
  - use `chtop` only if a small telemetry strip or sparkline-style health pane adds real operator value

**Design constraints from the matrix**
- Keep Codero review- and operator-first; do not drift into a chat-first shell
- Reuse ideas, not upstream taxonomy or branding
- Favor permissive-license repos for implementation patterns; keep copyleft repos as inspiration only
- Preserve the good Codero-specific surfaces already in place:
  - review assistant shell
  - findings & routing pane
  - pipeline pane
  - event stream & architecture pane

**Remaining work driving the next slice (2026-03-20)**
- Fix the event stream first: SSE / `DeliveryEvent` ingestion is still not flowing, so the log pane is empty downstream.
- Add a persistent merge-status footer: `BlockReason` is still missing from the always-visible bottom bar.
- Bind live agent row state: per-agent status, elapsed time, and finding counts still render as static or placeholder values.
- Complete findings detail: `FindingRecord` still needs file:line rendering, and CRITICAL/HIGH routing is incomplete.
- Make the assistant context-aware: gate/finding context is not injected, and `ChatSuggestion` chips still do not render.
- Close the missing panes: findings detail tab and run history tab are still not started.
- Finish the TUI footer polish: persistent keybinding hints and `EventType` color-coding are still absent.

**Pilot reference:** P3 + P5 evidence from Pilot Rerun Batch 2

---

### UI-002: Dashboard UI Component Refresh (P1)
**Surface:** Dashboard (web)
**Owner:** ClaudeCode / UI agent
**Status:** Not started
**Description:** Refresh the dashboard frontend components for gate-check and gate-status views.
Current components are functional; this targets visual consistency with the updated TUI aesthetic.

**Acceptance criteria:**
- Dashboard gate-checks panel uses same status icon/colour semantics as TUI refresh
- `/api/v1/dashboard/gate-checks` response schema unchanged (non-breaking)
- Dashboard fixture (`--serve-fixture`) continues to serve valid fixture data
- Existing dashboard contract tests pass unchanged

---

### INFRA-001: Distinguish `/gate` (heartbeat) from `/gate-check` (local checks) surface naming (P2)
**Surface:** Dashboard API
**Owner:** OpenCode / API agent
**Status:** Not started
**Description:** The `/gate` endpoint returns AI review gate heartbeat status; the
`/api/v1/dashboard/gate-checks` endpoint returns local pre-commit gate-check results.
These coexist correctly but their naming is non-obvious to new operators.

**Acceptance criteria:**
- API contract documentation updated to clarify both endpoints with usage examples
- Optional: add a `/api/v1/dashboard/gate-check` alias endpoint for clarity
- No breaking changes to existing consumers

---

### LOG-001: Structured heartbeat log definition for per-step UI rendering (P1)
**Surface:** Observability contract / heartbeat / UI consumers
**Owner:** OpenCode / API agent
**Status:** Not started
**Description:** Define a machine-readable log and heartbeat schema that emits
`passing`, `failing`, and `disabled` for each individual step in the review pipeline,
including the current 12-21-step pre-commit script matrix, so the TUI and dashboard can
render state directly instead of inferring it from freeform text or overall run status.
The dashboard should consume the canonical gate-check report envelope (`/api/v1/dashboard/gate-checks`)
instead of a separate step-only endpoint.

**Acceptance criteria:**
- Canonical schema documented in `docs/contracts/` for per-step heartbeat/log events
- Canonical dashboard feed remains the existing `gate-checks` report envelope; no parallel
  `gate-steps` route is introduced
- Every pipeline step emits a stable status, duration, and reason payload
- Step ordering and identifiers are stable enough for UI rendering
- TUI and dashboard adapters render the same per-step states from the same source
- Disabled steps preserve reason codes and human-readable reasons
- Existing overall gate `PASS` / `PENDING` / `FAIL` behavior remains backward compatible

---

### UI-003: Current Activity Session Panel for the GUI (P1)
**Surface:** Dashboard / runtime activity
**Owner:** ClaudeCode / UI agent
**Status:** Not started
**Description:** Add a live activity panel that shows which sessions are currently active
and, when available, what task or phase each session is working on. The GUI should use
structured session telemetry rather than inferring state from logs alone.

**Acceptance criteria:**
- Dashboard shows active sessions with session ID, repo, branch, owner/agent, elapsed time, and last heartbeat
- Current task or phase is shown when available; otherwise the UI shows a clear fallback like `unknown`
- Active-session data refreshes on the existing poll cadence or via live updates
- Expired or inactive sessions are not mixed into the active list
- No secrets or credentials are exposed in the panel

---

### DOC-001: Gate-check activation guide (P2)
**Surface:** Docs
**Owner:** Any agent
**Status:** Not started
**Description:** Add a concise operator guide explaining how to activate optional gate checks
(`forbidden-paths`, `lockfile-sync`, `exec-bit-policy`) with both required env vars.

**Acceptance criteria:**
- New `docs/runbooks/gate-check-activation.md` file
- Covers each optional check: env vars, example values, expected output
- Cross-referenced from `gate-check-env-contract-v1.md`

---

---

### COD-NEW-A: Post-Push CI Watcher (P1)
**Surface:** Agent toolkit — `ci-watch.sh`
**Owner:** ClaudeCode
**Status:** Completed (merged PR #60)
**Surfaced by:** TEST-2 local gate pilot (2026-03-18) — CI failure post-push had no feedback path back to agent

**Description:** After `git push`, Codero has no mechanism to monitor GitHub Actions status and
surface CI failures back to the agent in-session. The agent only learns of CI failures by
manually polling `gh run list`. This item adds a `ci-watch.sh` script to the shared
agent-toolkit that polls CI status until completion and delivers a structured pass/fail report
with failure log excerpts.

**Acceptance criteria:**
- `ci-watch.sh [branch] [timeout_seconds]` polls `gh run list` every 15s until all runs complete or timeout
- On success: prints summary table of all checks with durations
- On failure: prints failing job names + first 40 lines of failure log for each failing job
- Exit code 0 for all-pass, 1 for any failure, 2 for timeout
- Installed at `/srv/storage/shared/agent-toolkit/bin/ci-watch.sh`
- Integrated into `agent-finish-task` workflow (optional, post-push poll)

---

### COD-NEW-B: Pre-Push Test Gate (P1)
**Surface:** Git hook — `pre-push`
**Owner:** ClaudeCode
**Status:** Completed (merged PR #60)
**Surfaced by:** TEST-2 local gate pilot (2026-03-18) — `TestIntentionalFail` passed pre-commit
(which does not run `go test`) and reached CI undetected

**Description:** The pre-commit hook runs gitleaks, semgrep, and the AI review gate, but does NOT
run `go test ./...`. A deliberately broken test therefore sails through pre-commit and only fails
in GitHub Actions CI 30–60 seconds later. This item adds a `pre-push` hook that runs the full
Go test suite before any push is allowed to reach the remote, closing the gap between
pre-commit and CI.

**Acceptance criteria:**
- `/srv/storage/shared/agent-toolkit/hooks/pre-push` runs `go test ./...` for Go repos (detected via `go.mod` at repo root)
- Push is blocked (exit 1) if any test fails; full test output shown
- Push proceeds (exit 0) if all tests pass
- Hook is a no-op for non-Go repos
- Symlinked from `.githooks/pre-push` via `install-hooks`
- `install-hooks` script updated to install `pre-push` alongside `pre-commit`

---

---

### COD-NEW-C: Autonomous Finish-Loop Orchestrator (P0)
**Surface:** Agent toolkit — `codero-finish.sh`
**Owner:** ClaudeCode
**Status:** Completed (merged PR #62)
**Surfaced by:** Full-cycle pilot (2026-03-18) — agent still manually drove each step
(commit, push, PR open, ci-watch call, CodeRabbit poll, feedback read, re-commit).
The intent is for Codero to own all of that once the agent says "I'm done."

**Description:** `codero-finish.sh "<commit-message>"` is the single handoff point.
The agent writes code, stages changes, then calls this script. Codero takes over:

1. **Commit** — runs `git commit` triggering pre-commit gates in repo-default order
   (as defined by the orchestrator, e.g. `two-pass-review.sh`): gitleaks, path-guard,
   Semgrep (always a mandatory blocking gate), AI gate. If blocked, returns exact gate
   output as structured feedback and exits 3.
2. **Push** — runs `git push` triggering pre-push (`go test ./...`).
   If blocked, returns test failure output and exits 4.
3. **PR** — creates PR if none exists (with `@coderabbitai review` in body).
   If PR exists, posts `@coderabbitai review` comment to trigger fresh review.
4. **CI watch** — polls `gh run list` until all runs complete (via `ci-watch.sh`).
   Hard-fails only on non-infrastructure failures (sonarcloud infra failures ignored).
5. **CodeRabbit poll** — waits up to 10 minutes for CodeRabbit to complete its review.
6. **Feedback extraction** — parses actionable inline comments from the PR review thread.
7. **Deliver** — writes `.codero/finish-feedback.md` + prints to stdout.
   Exit 0 = merge-ready (all clean). Exit 1 = feedback for agent to act on.

Agent addresses feedback, re-stages, calls `codero-finish.sh` again. Loop repeats
until exit 0 (all gates pass, CI clean, no actionable CodeRabbit comments).

**Acceptance criteria:**
- Installed at `/srv/storage/shared/agent-toolkit/bin/codero-finish.sh`
- Exit 0: all gates pass + CI clean + 0 actionable CodeRabbit comments
- Exit 1: structured feedback in `.codero/finish-feedback.md` and stdout
- Exit 2: CI non-infrastructure failure with log excerpt
- Exit 3: pre-commit blocked with exact gate output
- Exit 4: pre-push blocked with test failure output
- Feedback file is cleared on clean exit
- Does not require agent to manually call `ci-watch.sh`, open PR, or poll CodeRabbit

---

### COD-055: Finish-Loop Stabilization Follow-Up (P0)
**Surface:** Agent toolkit — `codero-finish.sh`
**Owner:** ClaudeCode
**Status:** In progress (PR #63 open)
**Surfaced by:** CODERO-E2E-CTX-001 — Phase 5 timed out because CodeRabbit completion was only
checked through `reviews[]`, while CodeRabbit sometimes finished via PR issue comments.
The same run also staged `SCRATCHPAD.md` because auto-stage relied on `git add -u` without a
pre-commit purge.

**Description:** Hardened the finish loop so CodeRabbit completion is detected from both the
review thread and the PR comment stream, and so scratchpad files are removed from the staged
set before commit every time.

**Acceptance criteria:**
- CodeRabbit terminal completion is detected from both review records and PR comments
- `SCRATCHPAD.md` cannot be auto-staged or committed by the finish loop
- Merge-ready runs return exit `0` when CI is green and CodeRabbit reports no actionable comments
- The finish loop still produces actionable feedback when CodeRabbit does have findings

---

## Entry Criteria for v1.2.4
- v1.2.3 released and tagged
- All v1.2.3 pilot evidence archived
- UI-001 and UI-002 designs reviewed and approved before implementation

## Exit Gate for v1.2.4
- `make lint` passes
- `go test -count=1 ./...` passes
- All existing parity tests pass (surface contract must not regress)
- Pilot 5 (surface parity) reruns successfully on updated surfaces
- PR reviewed and approved (no blocking CodeRabbit comments)
