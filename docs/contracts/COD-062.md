# COD-062 — Docker Runtime Home Dir Cleanup

Status: in-progress
Owner: codex

## Summary

- Removed the Docker runtime user home-dir warning during image build.
- Switched the runtime user home from `/nonexistent` to `/data/runtime/<runtime-username>` so the path is explicit and verifiable.
- Kept the runtime layout otherwise unchanged.

## Contract Artifacts

- `Dockerfile`
- `docs/agent-task-board.md`

## Notes

Validation is build-focused:

- Docker image build should complete without the prior `adduser --home /nonexistent` warning.
- The runtime user should still be `codero`, with the home path resolved as `/data/runtime/<runtime-username>`.
- The standard Go binary build should remain unchanged.

Verified:

- `GOFLAGS=-buildvcs=false make build`
- `docker build --build-arg VERSION=$(git rev-parse --short HEAD) -t codero:cod062 .`
- The Docker build now emits a normal home-directory creation line instead of the prior `Not creating home directory` line.
