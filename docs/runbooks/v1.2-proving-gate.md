# Codero v1.2 Proving Gate — Runbook

## Overview

`codero prove` is the deterministic v1.2 proving entrypoint.  It runs all
high-signal checks in a single pass and emits both a human-readable summary table
and a stable JSON object.  It is the canonical command for CI evidence capture and
daily operator proving verification.

---

## Command Invocation

```bash
# Standard run (includes race tests — takes ~2 min)
codero prove

# Fast mode (skips C-004 race tests — ~15 seconds)
codero prove --fast

# JSON output only (suppress human table; JSON goes to stdout)
codero prove --json

# With running daemon (enables observability smoke checks C-007..C-009)
codero prove --endpoint-url http://localhost:8080

# Non-default repo root
codero prove --repo-path /path/to/codero/repo
```

### Exit codes

| Code | Meaning |
|---|---|
| `0` | All executed checks PASS (SKIP and TODO are not failures) |
| `1` | One or more checks returned FAIL |

---

## Output Schema

JSON is written to **stdout** (so it can be captured by CI).  Human table is
written to **stderr**.  Schema version is `"1"`; additions are additive only.

```json
{
  "schema_version": "1",
  "timestamp": "2026-03-17T00:57:41Z",
  "version": "v1.2.0",
  "repo_path": "/srv/storage/repo/codero",
  "overall_status": "PASS",
  "total": 22,
  "passed": 14,
  "failed": 0,
  "skipped": 4,
  "todo_count": 4,
  "failing_checks": [],
  "checks": [
    {
      "id": "C-001",
      "name": "go-build",
      "appendix_ref": "",
      "status": "PASS",
      "detail": "OK",
      "duration_ms": 1860
    }
  ]
}
```

### `status` values for individual checks

| Value | Meaning |
|---|---|
| `PASS` | Check executed and passed |
| `FAIL` | Check executed and failed |
| `SKIP` | Check not applicable (e.g. daemon not running for smoke check) |

---

## Check Catalogue

| ID | Name | Appendix ref | Notes |
|---|---|---|---|
| C-001 | go-build | — | `go build ./...` in repo root |
| C-002 | go-vet | — | `go vet ./...` |
| C-003 | unit-tests | Appendix G: all drills | `go test -count=1 ./...` — 19 packages |
| C-004 | race-tests | — | `go test -race ./...`; skipped with `--fast` |
| C-005 | non-tty-gate-json | COD-027 | `gate-status --json` non-TTY contract validation |
| C-006 | non-tty-watch | COD-027 | `gate-status --watch` non-TTY fallback to JSON |
| C-007 | obs-health | Appendix F: /health | HTTP GET smoke; SKIP when daemon not running |
| C-008 | obs-gate | Appendix F: /gate | HTTP GET smoke; SKIP when daemon not running |
| C-009 | obs-dashboard | Appendix F: /dashboard/ | HTTP GET smoke; SKIP when daemon not running |
| C-010 | state-db-error | Appendix F: state paths | Verifies non-writable DB path returns actionable error |
| C-011 | drill:G-001-redis-startup | Appendix G | Covered by TestRedisUnavailableAtStartup (C-003) |
| C-012 | drill:G-002-redis-transient | Appendix G | Covered by TestIntegration_RedisRestart (C-003) |
| C-013 | drill:G-003-redis-restart-mid-dispatch | Appendix G | Covered by TestIntegration_RedisRestart_SeqNoRegression (C-003) |
| C-014 | drill:G-004-sigterm | Appendix G | Covered by HandleSignals wg-drain path (C-003) |
| C-015 | drill:G-005-sigkill-recovery | Appendix G | Covered by TestIntegration_LeaseExpiryDuringReview (C-003) |
| C-016 | drill:G-006-lease-expiry | Appendix G | Covered by TestIntegration_LeaseExpiryDuringReview (C-003) |
| C-017 | drill:G-007-duplicate-webhook | Appendix G | Covered by TestDeduplicator (C-003) |
| C-018 | drill:G-008-out-of-order-webhook | Appendix G | Covered by idempotency tests (C-003) |
| C-019 | drill:G-009-missing-keyspace-notifications | Appendix G | Covered by TestExpiryWorker_LeaseAudit_Requeue+Blocked (C-003) |
| C-020 | drill:G-010-circuit-breaker-redis-unavailable | Appendix G | Covered by TestCheckRedis_FailsWithNamedError (C-003) |
| C-021 | drill:G-011-seq-gap | Appendix G | Covered by TestStream_Replay_ToleratesSeqGap (C-003) |
| C-022 | drill:G-012-compaction | Appendix G | Covered by TestStream_InitSeqFloor_PreventsRegression (C-003) |

---

## Pass Criteria

A run is considered passing when:

- `overall_status == "PASS"` in the JSON output
- `failed == 0`
- `failing_checks` is empty (`[]`)
- SKIP checks (daemon not running, `--fast` race skip) are **not** failures

---

## Appendix G Drill Coverage Map

| Appendix G case | Check IDs | Status |
|---|---|---|
| Redis unavailable at startup | C-011 → C-003 | ✅ PASS |
| Redis transiently unavailable after startup | C-012 → C-003 | ✅ PASS |
| Redis restart mid-dispatch | C-013 → C-003 | ✅ PASS |
| SIGTERM graceful shutdown | C-014 → C-003 | ✅ PASS |
| SIGKILL + orphaned lease repair | C-015 → C-003 | ✅ PASS |
| Lease key expired | C-016 → C-003 | ✅ PASS |
| Duplicate webhook delivery | C-017 → C-003 | ✅ PASS |
| Out-of-order webhook arrival | C-018 → C-003 | ✅ PASS |
| Missing keyspace notifications | C-019 → C-003 | ✅ PASS |
| Redis unavailable during pre-commit circuit-breaker check | C-020 → C-003 | ✅ PASS |
| Crash between seq increment and append | C-021 → C-003 | ✅ PASS |
| Compaction — durable seq floor | C-022 → C-003 | ✅ PASS |

---

## Sample Output (--fast run, daemon not running)

```text
Codero v1.2 Proving Gate — 2026-03-17T00:57:41Z
Version: v1.2.0  Repo: /srv/storage/repo/codero

  ID       NAME                                     STATUS       DUR  DETAIL
  -------  ---------------------------------------  ------  --------  ------
  C-001    go-build                                 PASS     1860ms  OK
  C-002    go-vet                                   PASS      756ms  OK
  C-003    unit-tests                               PASS    11243ms  19 packages passed
  C-004    race-tests                               SKIP        0ms  skipped: --fast flag set
  C-005    non-tty-gate-json                        PASS        0ms  valid JSON, all required keys present
  C-006    non-tty-watch                            PASS        0ms  non-TTY fallback produces valid JSON, exit 0
  C-007    obs-health                               SKIP        1ms  daemon not reachable (expected when not running)
  C-008    obs-gate                                 SKIP        0ms  daemon not reachable (expected when not running)
  C-009    obs-dashboard                            SKIP        0ms  daemon not reachable (expected when not running)
  C-010    state-db-error                           PASS        0ms  OS returns actionable error for non-writable DB path
  C-011    drill:G-001-redis-startup                PASS        0ms  Covered by TestRedisUnavailableAtStartup → C-003
  C-012    drill:G-002-redis-transient              PASS        0ms  Covered by TestIntegration_RedisRestart → C-003
  C-013    drill:G-003-redis-restart-mid-dispatch   PASS        0ms  Covered by TestIntegration_RedisRestart_SeqNoRegression
  C-014    drill:G-004-sigterm                      PASS        0ms  Covered by HandleSignals wg-drain path → C-003
  C-015    drill:G-005-sigkill-recovery             PASS        0ms  Covered by TestIntegration_LeaseExpiryDuringReview
  C-016    drill:G-006-lease-expiry                 PASS        0ms  Covered by TestIntegration_LeaseExpiryDuringReview
  C-017    drill:G-007-duplicate-webhook            PASS        0ms  Covered by TestDeduplicator → C-003
  C-018    drill:G-008-out-of-order-webhook         PASS        0ms  Covered by idempotency tests → C-003
  C-019    drill:G-009-missing-keyspace-notifications PASS      0ms  Covered by TestExpiryWorker_LeaseAudit → C-003
  C-020    drill:G-010-circuit-breaker-*            PASS        0ms  Covered by TestCheckRedis_FailsWithNamedError → C-003
  C-021    drill:G-011-seq-gap                      PASS        0ms  Covered by TestStream_Replay_ToleratesSeqGap → C-003
  C-022    drill:G-012-compaction                   PASS        0ms  Covered by TestStream_InitSeqFloor_PreventsRegression → C-003

Overall: PASS  (18 passed, 0 failed, 4 skipped, 0 TODO)
```

---

## Troubleshooting

### C-001 / C-002 / C-003 FAIL

Go toolchain check failed.

1. Verify Go version: `go version` — must be `go1.24.2` or compatible.
2. Run manually: `go build ./...` and inspect the error output in the `detail` field.
3. If missing modules: `go mod download && go mod tidy`.
4. Ensure you are running `codero prove` from the repo root (or pass `--repo-path`).

### C-007 / C-008 / C-009 show FAIL (not SKIP)

The daemon is running but returning a non-2xx/3xx response.

1. Check daemon logs: `codero status`
2. Run `codero dashboard --check` for detailed endpoint diagnostics.
3. Verify port binding: `codero ports`
4. If behind a reverse proxy, pass `--endpoint-url https://your-proxy-base`.

---

## CI Integration

Capture JSON evidence in CI:

```bash
# Capture JSON to file; human summary goes to stderr (visible in CI logs)
codero prove --fast 2>prove-human.txt | tee prove-result.json
echo "Overall: $(jq -r .overall_status prove-result.json)"
```

Or fail the CI step on any FAIL:

```bash
codero prove --fast   # exits 1 if any FAIL, 0 if all PASS/SKIP/TODO
```
